#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_PRE_PUSH_CHECKS:-0}" == "1" ]]; then
    echo "Skipping pre-push checks (SKIP_PRE_PUSH_CHECKS=1)."
    exit 0
fi

echo "Running TLA pre-push checks..."
composer test:parallel
composer test:types
composer test:rector
